<%= node['mongodb']['log_dir'] %>/<%= node['mongodb']['nodename'] %>.log
{
        rotate 4
        weekly
        copytruncate
        compress
        delaycompress
        sharedscripts
        postrotate
        ##  2.0.3 or later, don't use USR1.
        #   kill -USR1 `cat /var/run/<%= node['mongodb']['nodename'] %>.pid` >/dev/null 2>&1 || true
        endscript
}

